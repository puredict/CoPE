# Experiment 1 claim decision

Decision: `BLOCKED_FORMAL_READINESS`.

The catalog completeness audit passes, the production learned VLA and reasoner are independently qualified, and pilot preflight selects tasks 1 and 4. The scientific run cannot begin: only four tasks are eligible against the frozen minimum of eight; the production public event-evidence builder, public verifier, nominal planner, and full `RuntimeAssembly` are unavailable; the interrupted-episode budget lacks admissible calibration evidence; and the 32-cell production pilot, comparator selection, and formal freeze have not occurred.

Formal provider calls are 0 and formal trajectories are 0. Pilot trajectories are also 0. Therefore there is no behavioral comparison and no paper result to score as `GO`, `PARTIAL_SUPPORT`, or `NO_GO`.

Software-supported statements are limited to the frozen trace contract, production adapter identities, 100-trajectory clean calibration, source-backed catalog closure, zero-provider semantic feasibility, sealed structural replay, and tested runtime/journal invariants. Claims of downstream recovery improvement, lower history corruption, flatter K-scaling, generic-persistent non-inferiority, learned open-world discovery, or end-to-end latency remain unsupported.

Precise blockers are listed in `FORMAL_READINESS_GATES.csv`; the first runnable production step is the guarded pilot command in `NEXT_COMMAND.md`.
