CoPE secure credential handoff
Date: 2026-08-04

The OpenRouter key previously pasted in chat must be revoked/rotated. Do not
reuse it. Create a new key and enter it only through getpass under the personal
lijingsu account. The helper passes it to one detached child environment; it
does not place the value in argv, shell history, Git, or the launch log.

1. SSH to port 26575 as lijingsu and verify the branch is clean:

cd /home/lijingsu/codex-worktrees/cope-shared-envelope
git branch --show-current
git status --porcelain

Required branch: codex/shared-commit-envelope
Required status output: empty

2. Launch the single-draw smoke. The command will silently prompt for the new
secret once:

/home/lijingsu/codex-worktrees/cope-runtime-python-20260803-v1/bin/python \
  tools/launch_with_secret_env.py \
  --env-name OPENROUTER_API_KEY \
  --cwd /home/lijingsu/codex-worktrees/cope-shared-envelope \
  --log /home/lijingsu/cope-runs/provider-smoke-v1.launch.log \
  -- \
  scripts/run_project_env.sh \
  /home/lijingsu/codex-worktrees/cope-runtime-python-20260803-v1/bin/python \
  experiments/provider_operational_smoke.py \
  --output-dir /home/lijingsu/cope-runs/provider-smoke-v1

3. Observe without retrying:

tail -f /home/lijingsu/cope-runs/provider-smoke-v1.launch.log

The only unlock condition is:

/home/lijingsu/cope-runs/provider-smoke-v1/00_STATUS.txt has gate PASS.

A FAIL, exit 2, or 00_CALL_INTENT.txt without 01_REDACTED_TRACE.txt consumes
the draw. Do not delete the directory, create provider-smoke-v2, or rerun.

4. After PASS, use the same getpass helper for the occurrence command in
research/143_END_TO_END_FORMAL_LAUNCH_CHECKLIST.md, with a distinct exclusive
launch log. Do not launch embodied v2 until occurrence analysis writes
04_DECISION.txt with joint_primary_gate=true and the exact preregistered PASS
claim status.

Security limit: a child process must hold the credential in its environment
because the frozen adapter reads OPENROUTER_API_KEY. Other processes with
permission to inspect the same user's process environment may be able to read
it. Use the personal account, not the shared gongyong account, and revoke the
new key after the formal calls finish.
