# X14 analysis lock

Frozen: 2026-08-03 while the detached provider run was active and before any
result file or partial model outcome was available.

The checked-in analysis program is authoritative. It retains all assigned
cells, verifies 180 provider-OK cells and 60 fair triplets, reports cell-level
rates descriptively, and treats the 12 base semantic templates as the clustered
comparison units.

GO to true-clean embodied preparation requires all of:

1. smoke expanded and all provider/fairness gates pass;
2. CoPE has more correct cells than compact TX;
3. more base templates favor CoPE than favor compact TX;
4. CoPE has no excess count of unauthorized/stale, progress-corruption or
   continuity failures relative to compact TX;
5. at least one CoPE-only cell occurs in a substantive recovery family rather
   than only no-op, authorization, version or idempotence controls.

The cell-level exact sign calculation is exploratory because variants within a
template are correlated. The cluster-level sign calculation excludes tied base
templates. Neither overrides the multidimensional GO rule.
